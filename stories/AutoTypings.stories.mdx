import { Meta } from '@storybook/addon-docs'
import { Demo1, Demo1Wrapper } from './autoTypingsDemo'

<Meta title="Example/AutoTypings" />

## AutoTypings feature

`Playground` component with `autoTypings` feature. It will automatically resolve typings for imported libraries.

the following code imported `lodash` and `your-private-lib`, you can check the type definitions of it.

<Demo1 />


## Code of 'AutoTypings feature' example

here is the code of 'AutoTypings feature' example.

you can see the `Playground`'s configuration.

<Demo1Wrapper />
